import { IonSelectFilterDirective } from './ion-select-filter.directive';

describe('IonSelectFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new IonSelectFilterDirective();
    expect(directive).toBeTruthy();
  });
});
