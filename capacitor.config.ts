import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tat-softs-ui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
